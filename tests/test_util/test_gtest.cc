#include "gtest/gtest.h"
namespace {

/**
 * 用于测试gtest是否已经生效
*/
TEST(UtilsTest, Gtest) {
    // The test is named "Gtest", and belongs to the "UtilsTest" test case.
    EXPECT_FALSE(false);
}

} // namespace